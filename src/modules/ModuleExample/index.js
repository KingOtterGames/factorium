const onUpdate = (state, _deltaTime) => {
    return state
}

const onFixedUpdate = (state) => {
    return state
}

const ModuleExample = {
    onUpdate,
    onFixedUpdate,
}

export default ModuleExample
